# Ticket 03: 歷史籌碼膠囊升級為動能時間窗切換 (1D/3D/5D) 與狀態列降級提示

## 狀態
- 狀態: `completed`
- 關聯規格: `docs/specs/0119-pledge-unified-repayment-and-smart-money-resilience-spec.md` (模組三)
- 關聯 Issue: #32

## 任務目標
在 `src/components/ChipsWorkspace.tsx` 頂部工具列中，移除原先無操作反饋的靜態 `💾 歷史籌碼 (5)` 徽章，升級為可切換的「動能時間窗膠囊 (Horizon Selector)」：`[1日 (當日)]` / `[3日 (短期)]` / `[5日 (週籌碼)]`；並在日報發生降級時，狀態列以黃燈誠實提示使用者。

## 具體修改清單
1. **`src/components/ChipsWorkspace.tsx`**：
   - 移除頂部純展示的 `💾 歷史籌碼 (5)` 靜態 DOM 元素。
   - 將 IndexedDB 快取天數資訊移入「同步按鈕」或狀態標籤的 hover Tooltip 中。
   - 導入動能時間窗選擇器狀態：`chipsHorizon: 1 | 3 | 5`（預設為 1）。
   - 在頂部工具列提供 `[1日]` / `[3日]` / `[5日]` 膠囊切換鈕，點選具備選取高亮狀態。
   - 狀態燈號支援降級提示：若當日日報不合格降級為 T-1，狀態徽章呈現：`🟡 09/11 盤後結算中 (暫以 09/10 呈現，共 1,894 檔)`。
2. **測試驅動開發**：
   - 驗證頂部成功渲染 1D / 3D / 5D 切換鈕，點擊能正確切換 `chipsHorizon` 狀態。
   - 驗證降級時顯示黃燈提示。

## 驗收標準
- [ ] 頂部不再出現靜態無反應之歷史籌碼徽章。
- [ ] 1D / 3D / 5D 點選流暢且有清晰的高亮效果。
- [ ] 降級狀態提示清晰明確。
