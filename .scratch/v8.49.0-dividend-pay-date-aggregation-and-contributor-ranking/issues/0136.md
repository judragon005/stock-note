# Issue #80 (Spec 0136): 股利收益日誌與現金流全景統計聚合引擎入帳日時序對齊、股息貢獻排行過濾與毛淨額對帳系統

- **GitHub Issue**: [#80](https://github.com/judragon005/stock-note/issues/80)
- **Status**: Ready for implementation (`ready-for-agent`)
- **PRD**: [PRD #0136](file:///d:/APP/股票紀錄/docs/specs/0136-dividend-log-view-pay-date-aggregation-and-contributor-ranking-spec.md)
- **ADR**: [ADR 0136](file:///d:/APP/股票紀錄/docs/adr/0136-dividend-log-view-pay-date-aggregation-and-contributor-ranking.md)
- **Labels**: `bug`, `ready-for-agent`
- **Branch**: `fix/80-dividend-pay-date-aggregation-and-contributor-ranking`

## 待辦清單 (Checklist)

- [x] 在 `dividendAggregator.ts` 導出 `getEffectiveDividendPayDate(trade: TradeRecord): string`。
- [x] 在 `types/dividend.ts` 的 `DividendSummaryReport` 擴充 `currentYearGrossTWD` 與 `currentYearTaxTWD`。
- [x] 重構 `aggregateDividendReport`：
  - [x] 年度現金流與 YoY 依 `effectivePayDate` 歸屬，解決 2024/2025 跨年漏計與偷跑。
  - [x] 月度分佈依 `effectivePayDate` 歸屬，解決 7 月除息 8 月入帳錯位問題。
  - [x] 股息貢獻排行榜累計嚴格過濾 `effectivePayDate <= currentDateStr`。
  - [x] TTM 滾動現金流改用 `resolveEffectiveDividendTaxAndNet` 完整計算稅費。
- [x] 編寫/更新 `dividendAggregator.test.ts` 單元測試（TDD 紅-綠循環）。
- [x] 在 `DividendLogView.tsx` 整合：
  - [x] KPI 卡片顯示「實領淨額」與「應發毛額 / 扣繳稅費」對照提示。
  - [x] 歷史明細表新增「連動當年度 / 全歷史」切換過濾器。
- [x] 驗證 `npm test` 100% 綠燈與 `npm run build` 0 錯誤。
