# Issue #04: 本地 IndexedDB 歷史籌碼增量補齊與持久化快取管線

## 狀態
`ready-for-agent`

## 說明
在 `smartMoneyFetcher.ts` 建立歷史增量補齊機制。每次啟動比對本地 IndexedDB 中的已存日報，對缺失的近期交易日發起補齊，以時間換資料，累積真實台股法人籌碼歷史。

## 驗收標準
1. 支援依日期快取多個交易日之 `T86` 資料。
2. 命中快取時不發出重複網路請求。
3. 容錯安全，網路異常時平滑降級。
