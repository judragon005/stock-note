# 05 — 掛載 Row 3 卡片 (10 ~ 15)

**What to build:**
將 `TermTooltip` 掛載至 Row 3 的 6 張卡片：
1. **10 AI 多空能量棒 (`BullBearEnergyCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 多方能量、空方能量、20日紅黑K多空量能比。
2. **11 健康度綜合評估表 (`HealthSummaryCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 5 環指標：籌碼健康度、技術結構度、資金動能度、波動風險度、法人支撐度與綜合評分。
3. **12 AI 主力動態信號判斷 (`DynamicSignalsCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 5 行指示器：趨勢、籌碼、動能、風險、信號燈號。
4. **13 台股市場情緒儀表板 (`MarketSentimentCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 散戶情緒、法人情緒、主力情緒與市場情緒指針。
5. **14 AI 信心維度 (`AiConfidenceCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 總信心百分比、模型準確度、資料完整度、訊號穩定度、策略適用度。
6. **15 籌碼異動摘要 (`ChipsSummaryCard.tsx`)**：
   - 卡片標題 `ⓘ` 圖示。
   - 外資、投信、自營商進出張數、微型走勢 Sparkline、短線籌碼結論。

**Blocked by:** 04-mount-row2-cards

**Status:** completed

- [x] Card 10 多空能量比掛載 Tooltip
- [x] Card 11 健康度 5 環指標掛載 Tooltip
- [x] Card 12 主力動態信號判斷掛載 Tooltip
- [x] Card 13 市場情緒指針與三方情緒掛載 Tooltip
- [x] Card 14 AI 信心維度掛載 Tooltip
- [x] Card 15 籌碼異動摘要掛載 Tooltip
- [x] 既有單元測試綠燈驗證
