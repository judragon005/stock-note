# 任務工單 (Ticket #0139)：質押借貸 FULL_PAYOFF 全額結清統一委託法定沖償引擎、流通股數推導純函式集中化與持倉雙階排序 DRY 閉環重構

- **關聯 Issue**：[GitHub Issue #25](https://github.com/judragon005/stock-note/issues/25)
- **規格文檔**：[PRD #0139](file:///d:/APP/股票紀錄/docs/specs/0139-unified-full-payoff-and-engine-dry-refactor-spec.md)
- **架構決策**：[ADR 0139](file:///d:/APP/股票紀錄/docs/adr/0139-unified-full-payoff-and-engine-dry-refactor.md)
- **狀態**：`CLOSED` (已完成)
- **負責角色**：Agent / Developer

---

## 🎯 任務目標

1. **質押借貸 FULL_PAYOFF 全額結清分支統一委託 applyDebtRepayment 引擎 (Debt #0036 / Issue #25)**：
   - 淘汰 `src/components/CashLedgerWorkspace.tsx` L471~L533 的舊版手工拼裝邏輯。
   - 計算全額應清償總金額後，全面改為調用 `applyDebtRepayment` 核心純函式，嚴格遵照《民法》第 323 條（規費 ➔ 利息 ➔ 本金）進行法定沖償與拆分流水生成。
   - 確保更新後之借貸合約本金精確為 0、設質未繳規費精確清零，並標記結清日與最後繳息日。
2. **提取流通股數推導共用純函式 deriveSharesOutstanding (Debt #0038)**：
   - 於 `src/engine/keyMetricsEngine.ts` 導出 `deriveSharesOutstanding` 純函式。
   - 集中「資本額 / 10 ➔ 稅後淨利 / EPS ➔ 保底 10 億股」三階推導，消除 `calculateFcfYield`、`calculateDcfValuation` 與 `AnalysisMetricView.tsx` 內的重複代碼。
3. **持倉雙階自然排序 DRY 集中化重構 (Debt #0001)**：
   - 於 `src/utils/holdingsSort.ts` 導出 `compareHoldingsOrder` 純比較函式。
   - 供 `src/engine/calculator.ts` 與 `src/components/HoldingsTable.tsx` 統一調用，徹底終結積存歷史最久的 DRY 技術債。

---

## 驗收條件 (Acceptance Criteria)

- [x] 於 `src/utils/holdingsSort.test.ts` 驗證台美股排序與字典序自然升冪。
- [x] 於 `src/engine/keyMetricsEngine.test.ts` 驗證 `deriveSharesOutstanding` 覆蓋手動覆寫、資本額推導、EPS 反推與保底情境。
- [x] 於 `src/components/CashLedgerWorkspace.tsx` 執行 `FULL_PAYOFF` 時，借貸合約本金歸零、規費清零、利息全結，且帳本拆分流水與 `applyDebtRepayment` 100% 一致。
- [x] 既有測試全面綠燈（103 個測試套件、982 個測試 100% 通過），TypeScript 0 錯誤。
- [x] 技術債看板 `docs/debts/README.md`、`0001`、`0036`、`0038` 狀態更新為 `RESOLVED`。
