# 2026-09-10 V8.35.0 Web Crypto 敏感金鑰加密、CORS 代理零憑證防洩漏與 CSV DDE 消毒交付手冊

## 一、版本核心目標 (Release Goal)
本版本完整實作 **PRD #0116**，徹底解決三大高優先級資安技術債（**Debt #0030**, **Debt #0031**, **Debt #0032**）：
1. **Web Crypto 敏感金鑰端到端加密 (Debt #0030)**：使用瀏覽器原生 W3C `window.crypto.subtle` (AES-GCM 256-bit + PBKDF2 100,000 次疊代) 加密存儲使用者 API 金鑰，支援原地自動遷移 (Auto-Migration) 與防窺遮罩 UI。
2. **CORS 代理零憑證防洩漏與安全邊界路由 (Debt #0031)**：實作 `secureProxyRouter` 路由中介層，偵測敏感憑證參數（token/apikey/auth）並 100% 阻斷外發至公共 CORS 代理池；同時對自訂 Proxy 進行 HTTPS 校驗與私有內網/雲端 Metadata SSRF 阻斷。
3. **CSV DDE 公式注入防禦與備份脫敏 (Debt #0032)**：實作 OWASP 標準儲存格消毒過濾器（前置單引號 `'` 中和 `=, +, -, @, \t, \r` 危險字元），並提供全庫 JSON 備份脫敏匯出功能。

---

## 二、變更模組與關鍵檔案 (Architectural Artifacts)

| 模組名稱 | 關鍵檔案 | 核心職責 |
| :--- | :--- | :--- |
| **Web Crypto 密碼學引擎** | `src/engine/cryptoEngine.ts`<br/>`src/engine/cryptoEngine.test.ts` | 原生 AES-GCM 256-bit 加解密、PBKDF2 100,000 次金鑰衍生、16-byte Salt、12-byte IV、Base64 封裝與明文平滑遷移。 |
| **零憑證安全路由與 SSRF 防禦** | `src/engine/secureProxyRouter.ts`<br/>`src/engine/secureProxyRouter.test.ts` | 審查 HTTP 請求敏感憑證字典，強制排除公共 CORS 代理池；驗證自訂 Proxy HTTPS 與阻斷內網/雲端 Metadata SSRF。 |
| **OWASP CSV 消毒與全庫備份脫敏** | `src/engine/csvSanitizer.ts`<br/>`src/engine/csvSanitizer.test.ts` | 消毒 `=, +, -, @, \t, \r` 危險字元防範 Excel DDE 公式注入；全庫備份抹除金鑰並標記 `isRedacted: true`。 |
| **外部請求加固** | `src/engine/priceFetcher.ts`<br/>`src/engine/corporateActionScanner.ts` | 在發起外部公共 CORS 代理前，強制攔截檢測憑證，有憑證即拋出 `SecurityCredentialRoutingError` 拒絕外發。 |
| **設定工作區 UI 整合** | `src/components/SettingsWorkspace.tsx` | 自訂 Proxy 即時 SSRF 檢核與紅字提示；備份匯出面板新增「🛡️ 脫敏匯出」核取方塊；API 金鑰標註「Web Crypto 256-bit 保護中」。 |
| **全庫資料庫匯出** | `src/utils/db.ts` | `exportFullDatabaseJSON(redactSensitive)` 支援脫敏參數。 |
| **交易 CSV 匯出加固** | `src/utils/storage.ts` | `exportTradesToCSV` 全面採用 `sanitizeCSVCell` 消毒輸出。 |

---

## 三、驗收數據與測試覆蓋 (Verification Metrics)

- **單元測試狀態**：**69 個測試檔案、736/736 個測試全數綠燈通過**（新增 24 個單元測試）。
- **靜態語法檢查**：TypeScript strict 模式 **0 錯誤**。
- **打包狀態**：Vite 生產模式打包耗時 10.86s 成功。
- **CI 流水線**：GitHub Actions PR #18 綠燈通過 (33s)。
- **PR 與 Issue**：[PR #18](https://github.com/judragon005/stock-note/pull/18) 經 Squash and Merge 合併至 `main`，[Issue #17](https://github.com/judragon005/stock-note/issues/17) 已自動關閉。

---

## 四、技術債解決狀態 (Tech Debts Closed)

- **Debt #0030** (`P1`): Web Crypto API 敏感金鑰加密與端到端保密持久化 ➔ `RESOLVED`
- **Debt #0031** (`P1`): 公共 CORS 代理憑證防洩漏與安全邊界路由機制 ➔ `RESOLVED`
- **Debt #0032** (`P2`): CSV 公式注入防禦 (DDE Protection) 與備份匯出脫敏機制 ➔ `RESOLVED`

---

## 五、後續建議行動 (Next Actions)
1. **下一期技術債候選**：
   - [**Debt #0033** (`P1`)](../debts/0033-csp-headers-and-browser-defense-hardening.md)：內容安全策略 (CSP) 與瀏覽器端防禦加固。
   - [**Debt #0034** (`P2`)](../debts/0034-import-parser-prototype-pollution-and-schema-validation.md)：匯入解析防護、原型污染防禦與數值邊界熔斷。
2. 啟動新需求或重構前，先輸入 `/grill-with-docs` 或 `/to-spec` 繼續推進。
