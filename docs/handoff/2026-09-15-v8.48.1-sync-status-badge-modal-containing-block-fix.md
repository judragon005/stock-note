# 交接文檔：V8.48.1 盤後同步彈窗 CSS 包含塊陷阱修復與視窗邊界自適應

## 1. 本次迭代概述

- **版本號**：V8.48.1
- **對應規格**：[Spec 0135](../specs/0135-sync-status-badge-modal-containing-block-fix-spec.md)
- **架構決策**：[ADR 0135](../adr/0135-sync-status-badge-modal-containing-block-fix.md)
- **關聯 GitHub Issue**：[#77](https://github.com/judragon005/stock-note/issues/77)
- **關聯 Pull Request**：[#78](https://github.com/judragon005/stock-note/pull/78) (Squash and Merged)
- **主分支 Commit**：`720c252`

---

## 2. 核心成果清單

1. **CSS 包含塊陷阱徹底修復 (Containing Block Trap Fix)**：
   - 解決 `MarketSyncStatusBadge.tsx` 位於 `<header className="glass-card">` 內部，其 `backdrop-filter: blur(16px)` 劫持 `position: fixed` 定位參照點，導致彈窗上半部被推擠至負座標（$Y \approx -200px$）並超出瀏覽器頂部視窗邊界的問題。
   - 引入 React 原生 `createPortal`，將彈窗根容器安全渲染至 `document.body` 頂層，徹底擺脫 Header 容器的 CSS 上下文。
2. **視窗上下邊界防溢出自適應 (`MODAL_VIEWPORT_STYLES`)**：
   - 遮罩外層：設定 `padding: 24px 16px` 與 `overflowY: auto`，保障上下均有足夠呼吸間隙且極矮螢幕下可整頁滾動。
   - 彈窗卡片本體：設定 `maxHeight: min(90vh, 620px)`、`overflowY: auto` 與 `margin: auto`，確保任何解析度與螢幕縮放比例下，彈窗標題、台美股市場卡片與關閉按鈕 `X` 均 100% 清晰可視可點。
3. **無障礙體驗提升 (Accessibility)**：
   - 註冊鍵盤事件監聽器，支援使用者按下 `Escape` 鍵退出彈窗，並在卸載與關閉時對稱釋放監聽器。
4. **品質保證與雙重驗證**：
   - 新增 `src/components/MarketSyncStatusBadge.test.ts`（6 項單元測試全數綠燈）。
   - 全站 101 個測試檔案、953 個單元測試 100% 通過。
   - TypeScript 編譯 0 錯誤，Vite Production Build 打包成功。
   - GitHub Actions CI 綠燈通過。
