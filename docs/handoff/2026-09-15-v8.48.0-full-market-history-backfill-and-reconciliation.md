# 交接文檔：V8.48.0 全市場全歷史數據回補與四層容錯修復管線

## 1. 本次迭代概述

- **版本號**：V8.48.0
- **對應規格**：[Spec 0134](../specs/0134-full-market-history-backfill-and-reconciliation-pipeline-spec.md)
- **架構決策**：[ADR 0134](../adr/0134-full-market-history-backfill-and-reconciliation-pipeline.md)
- **關聯 GitHub Issue**：[#73](https://github.com/judragon005/stock-note/issues/73)

---

## 2. 交付成果清單

1. **現存排程已知 Bug 修復**：
   - 修正 `scripts/market-sync/sync-tw-market.cjs` 中 TPEx T86 官方正確請求路徑（`daily_trade` + `o=json`），杜絕伺服器 302 重定向與 JSON 解析空字串報錯。
   - `fetchJson` 增加 HTTP 狀態碼防禦與空字串攔截。
   - 加入盤中未結算之友善提示，支援命令列自訂日期參數。
   - 修正 `scripts/market-sync/setup-windows-task.bat` 中引發 cmd 游標跳行之特殊字元。
2. **全市場全歷史回補排程核心 (`scripts/market-sync/backfill-local-csv.cjs`)**：
   - 讀取台股加權指數 7,158 個交易日大盤日曆作為 SSOT。
   - 高速串流剖析本機 2,359 檔個股與 485 檔三大法人歷史 CSV。
   - 實作「第二層停牌前值填補」：4,266 筆停牌無量紀錄自動沿用前日收盤價，成交量與籌碼補 0，指標連續無 NaN。
   - 實作「第一層差距稽核」：輸出 `backfill_gaps_audit.json`。
   - 本地 CPU 批次增量計算最新 MA5/10/20/60、RSI14、MACD、Darvas 箱體。
3. **雙目的地沉澱**：
   - `public/market-cache/tw_market_summary.json` (1.26MB) 供前端打開瞬間秒讀。
   - `public/market-cache/tw_market_ohlcv_compact.json` (16.7MB) 供前端非同步沉澱至 IndexedDB。
   - 驗收報告 `public/market-cache/backfill_audit_report.json`。
4. **批次檔設定中樞整合**：
   - 在 `setup-windows-task.bat` 選單中新增選項 `[5] 立即執行台股全市場全歷史回補`。
5. **品質與測試**：
   - 新增 `src/engine/backfillEngineCore.ts` 與 `src/engine/backfillLocalCsv.test.ts`。
   - 全量單元測試 100 個檔案、947 個測試 100% 綠燈。
   - TypeScript 編譯 0 錯誤、生產構建打包成功。
